#include <iostream>
#include <vector>

using namespace std;

int main() {

    int n;
    cin >> n;

    vector<pair<int, int>> A;
    int complaints = 0;
    vector<int>
    set


    for (int i = 0; i < 2*n; i++) {
        int c, l;
        cin >> c >> l;


        A.push({c, l});
    }



    return 0;
}





/*

complain when they go to somewhere theyve already been
kind of like intervals

10^5 customers


numrepeated at any time, then go through intervals

prefixes then edge cases

complaint for each time

times = location*2 = customers*2

they might be on at the time of the complaint, but they might not acrually make i
*/
